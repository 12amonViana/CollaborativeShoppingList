{
  "name": "shopping-list-functions",
  "private": true,
  "main": "lib/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "test:emulator": "npm run build && firebase emulators:exec --project demo-collaborative-shopping-list --config ../firebase.json --only auth,firestore,functions \"node test/emulator-e2e.mjs\"",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^14.3.0",
    "firebase-functions": "^7.3.2"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.2",
    "firebase": "^12.18.0",
    "firebase-tools": "^15.28.2",
    "typescript": "^5.7.2"
  }
}
